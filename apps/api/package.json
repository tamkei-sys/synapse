{
  "name": "@synapse/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "SYNAPSE API (Hono on Cloudflare Workers + tRPC v11).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc -b",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo .wrangler"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "@synapse/blocks": "workspace:*",
    "@synapse/schema": "workspace:*",
    "@trpc/server": "^11.0.0-rc.617",
    "better-auth": "^1.1.2",
    "drizzle-orm": "^0.36.4",
    "hono": "^4.6.10",
    "superjson": "^2.2.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^3.87.0"
  }
}
